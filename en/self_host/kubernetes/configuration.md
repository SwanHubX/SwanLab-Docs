# Custom Configuration

> This page is pending, covering detailed field descriptions and configuration examples for values.yaml.

<!-- Planned content:
- values.yaml full field reference (gateway / vector / service / dependencies)
- S3 object storage configuration (public/private bucket separation, endpoint notes)
- Node selection and affinity configuration
- Resource limits (resources) recommendations
- Multi-replica and high availability configuration
-->
